# Copy this file to `.env` and fill in the values. Do NOT commit your `.env` file.

# App configuration
# DATA_DIR: directory where users.json and ticket.json will be stored (defaults to project root)
DATA_DIR=
# APP_BASE: leave empty for root-hosted apps (recommended for local dev).
# Set to '/twig' (or another subpath) if you serve the app from a subdirectory.
APP_NAME=TicketFlow
APP_URL=http://localhost
APP_BASE=

# Session cookie settings
# In production set to true when using HTTPS
SESSION_COOKIE_SECURE=false

# Example notes:
# - To run locally on root using the built-in PHP server: APP_BASE= php -S 127.0.0.1:8000 index.php
# - On Render set APP_BASE=/ (or leave empty). The app is now zero-DB and uses users.json and ticket.json (stored in DATA_DIR or project root).
